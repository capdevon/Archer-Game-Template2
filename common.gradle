// Gradle settings and tasks common to all Archer-Game-Template2 subprojects

apply plugin: 'application' // to build JVM applications
apply plugin: 'java'        // to compile and test Java projects

java {
    sourceCompatibility = JavaVersion.VERSION_11
    targetCompatibility = JavaVersion.VERSION_11
}

import org.gradle.nativeplatform.platform.internal.DefaultNativePlatform
Boolean isMacOS = DefaultNativePlatform.currentOperatingSystem.isMacOsX()

tasks.withType(JavaExec).configureEach { // Java runtime options:
    if (isMacOS) {
        args '--noDialog'
        jvmArgs '-XstartOnFirstThread'
    }
    enableAssertions = true
    jvmArgs '--enable-native-access=ALL-UNNAMED' // suppress deprecation warnings
}

repositories {
    //mavenLocal()
    mavenCentral()
}

dependencies {
    implementation(rootProject.libs.heart)
    implementation(rootProject.libs.minie)
    implementation(rootProject.libs.skyControl)
    implementation(rootProject.libs.jme3.core)
    implementation(rootProject.libs.jme3.desktop)
    implementation(rootProject.libs.jme3.effects)
    implementation(rootProject.libs.jme3.plugins)

    runtimeOnly(rootProject.libs.jme3.awt.dialogs)
    runtimeOnly(rootProject.libs.jme3.jogg)
    runtimeOnly(rootProject.libs.jme3.lwjgl3)
    runtimeOnly(rootProject.libs.jme3.saferallocator)

    implementation files('../lib/shaderblow-ex-1.0.0.jar')
    runtimeOnly    files('../lib/jme-materialize.jar')
}

// cleanup tasks:

clean.dependsOn('cleanDLLs', 'cleanSOs')

tasks.register('cleanDLLs', Delete) { // extracted Windows native libraries
    delete fileTree(dir: '.', include: '*.dll')
}
tasks.register('cleanSOs', Delete) { // extracted Linux native libraries
    delete fileTree(dir: '.', include: '*.so')
}
